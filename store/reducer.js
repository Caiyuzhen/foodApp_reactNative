// 【🚀第二步】  定义一个初始化的存储数据
const defaultState = {
	name: "Dall.E"
}


export default (state = defaultState, action) => { //默认数据, 行为
	return state //🔥记得返回出数据！
}